# موقع تنزيل سبيكة سبأ

المجلد مستقل عن تطبيق العميل والإدارة.

## تجهيز الشعار
```powershell
cd E:\gold-hub\site
.\copy-brand-assets.ps1
```

## المعاينة
```powershell
cd E:\gold-hub\site
.\preview.ps1
```
ثم افتح http://localhost:8080

## رابط التنزيل
عدّل فقط `assets/js/config.js` بعد إنشاء GitHub Release. لا تضع أي Secrets داخل الموقع.
